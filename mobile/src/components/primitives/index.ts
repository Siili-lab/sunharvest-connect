/**
 * Primitive Components - Central Export
 *
 * Base UI components built on design tokens.
 * Use these instead of React Native components directly.
 *
 * Usage:
 *   import { Box, Text, Button, Input } from '@/components/primitives';
 */

export { Box } from './Box';
export { Text, Heading1, Heading2, Heading3, Body, Caption } from './Text';
export { Button } from './Button';
export { Input } from './Input';
